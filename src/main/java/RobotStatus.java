
public enum RobotStatus {
    IDLE,       // Waiting for a task
    WORKING,    // Actively performing a task
    CHARGING,   // At a charging station
    LOW_BATTERY // Idle, but needs to charge
}
